# RE:CONFIG 작업 지침

## 문서 책임

기획, 기믹, ROOM, 맵 문서를 만들거나 수정하기 전에는 반드시 [문서 책임 가이드](docs/DOCUMENTATION_GUIDE.md)를 읽는다.

- 공통 기믹의 동작·충돌·시각·기본 수치는 `docs/design/<기믹>.md`가 소유한다.
- ROOM 문서는 공간 구조, 배치, 경로 판단, 풀이 흐름, ROOM별 예외만 소유한다.
- 공통 사양을 ROOM 문서에 복사하지 않는다. ROOM에서는 공통 문서로 링크한다.
- 새 공통 기믹을 구현하면 공용 기믹 문서와 [기획 인덱스](docs/reconfig_design.md)를 같은 작업에서 갱신한다.
- 기존 규칙과 문서가 충돌하거나 공통 상호작용을 바꾸는 요청은 [구현 변경 규칙](docs/IMPLEMENTATION_RULES.md)을 따른다.

## 문서와 코드의 관계

문서와 코드가 다르면 임의로 한쪽을 정답으로 취급하거나 수치를 맞추지 않는다. 문서가 소유하는 범위, 현재 구현, ROOM 해법에 미치는 영향을 먼저 확인하고 필요한 승인 절차를 따른다.
## 문서 규칙 변경 금지

사용자가 명시적으로 요청하지 않은 문서 규칙, 사양, 제한, 예외를 임의로 추가하거나 수정하지 않는다. 특히 공통 기믹 문서에는 구현 추정이나 임의의 기획 판단을 규칙으로 기록하지 않는다. 기존 문서의 규칙 변경이 필요해 보이면, 변경 근거와 ROOM 해법·구현에 미치는 영향을 먼저 보고하고 승인을 받는다.

## ROOM 맵 코드 갱신 단축 요청

사용자가 현재 열려 있거나 명시한 `ROOM_XX_MAP.md`에 대해 “갱신”, “갱신 ㄱ”, “md에 맞춰”, “코드 갱신”처럼 짧게 요청하면, 이는 해당 ASCII 타일맵을 기준으로 ROOM 코드를 동기화하라는 요청이다. 어떤 변경을 찾았는지 되묻거나 일부 행만 추정해 반영하지 않는다.

`<번호> 갱신해`, `<번호> 갱신`, `ROOM <번호> 갱신`도 위 요청과 동일하다. 번호는 두 자리로 0을 채워 `docs/rooms/ROOM_<NN>_MAP.md`를 선택한다. 예를 들어 `2 갱신해`는 ROOM 02, `10 갱신`은 ROOM 10을 뜻한다. 번호가 명시되면 활성 문서보다 그 ROOM을 우선해 **전체 ASCII 타일맵 → 코드 동기화**를 즉시 수행한다. 규격·메모와 ASCII의 좌표 불일치를 설명하거나 기준을 물어보는 응답으로 끝내서는 안 된다.

1. [ROOM 맵 가이드](docs/rooms/ROOM_MAP_GUIDE.md)의 **ROOM 맵 → 코드 갱신 절차 (필수)**를 먼저 따른다.
2. 타일맵 전체와 ROOM 코드의 모든 배치 배열을 전수 대조해 `#`, 모든 오브젝트, 스위치/문, 피스톤을 함께 반영한다.
3. 코드 배치 좌표는 ASCII 마커를 기준으로 반영한다. 규격·오브젝트 메모가 다르면 갱신을 멈추지 말고 메모는 건드리지 않은 채 사후에 불일치만 한 줄로 보고한다. ASCII 자체가 규격을 만족하지 않거나 마커 해석이 불가능한 경우에만 기준을 확인받는다.
4. 단축 요청의 완료 조건은 전체 배치 동기화, 변경한 배치의 정적 충돌 대조, 빌드, `git diff --check`다. 전체 ROOM 실제 플레이·기존 문 경로 설계·ASCII에 없는 열린 위치의 추가 검토는 사용자가 별도로 요청할 때만 수행하며, 이들 때문에 동기화를 멈추거나 미완료로 보고하지 않는다. 보고에는 실제 실행한 검사 결과만 쓰며, 검증 도구가 산출하지 않은 타일 수·완전 일치·플레이 완료를 추정해 말하지 않는다.

이 단축 요청은 ROOM 배치 동기화만 뜻하며, 다른 ROOM·공통 기믹·문서 규칙 변경을 자동으로 승인하지 않는다.
